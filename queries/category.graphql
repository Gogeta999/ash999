query findCategory {
  categoriesByName(search: "Index") {
    id
    name
    notionId
    notionType
    isEnable
    parentCategory {
      id
      name
      notionId
      notionType
    }
  }
}
